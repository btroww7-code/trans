import request from 'supertest';
import { app } from '../src/main';

describe('User Journey', () => {
  it('should register, create listing, submit offer, accept offer', async () => {
    // ...register user, create listing, submit offer, accept offer...
  });
});
